// import RequestRepo from '../repositories/requestRepo';

// export const updateFinalDecision = async (requestId: number, finalDecision: string, jiraLinkOrComment: string): Promise<void> => {
//   try {
//     await RequestRepo.updateFinalDecision(requestId, finalDecision, jiraLinkOrComment);
//   } catch (error) {
//     console.error('Error updating final decision:', error.message);
//     throw error;
//   }
// };